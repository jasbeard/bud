// Available pathnames for the application
export enum AppPages {
  // Static routes
  HOME = "/",
  DASHBOARD = "/dashboard",
  BUDGET = "/budget",
  LOGIN = "/login",
  SIGNUP = "/signup",
  ONBOARDING = "/onboarding",
  ONBOARDINGPOC = "/onboarding poc1",
  // Dynamic routes - use template literal for dynamic parts
  BUDGETSPACE = "/budgetspaces",
}
